# Static Dashboard

This folder is designed for GitHub Pages.

- `index.html` is the static interactive dashboard.
- It is generated by `python src/build_static_dashboard.py`.
- GitHub Pages can serve this folder directly from the `main` branch `/docs` setting.
